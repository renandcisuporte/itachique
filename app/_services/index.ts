import { fetchAdvertisement } from "./fetch-advertisements";
import { fetchCategory } from "./fetch-categories";
import { fetchMetadata } from "./fetch-metadata";

const Api = {
  fetchAdvertisement,
  fetchMetadata,
  fetchCategory,
};

export default Api;
